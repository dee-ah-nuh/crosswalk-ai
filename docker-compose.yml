version: '3.8'
services:
  crosswalk:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/crosswalk.db
      - WAREHOUSE_ENABLED=false
    restart: unless-stopped
